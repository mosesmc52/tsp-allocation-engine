# TSP Tactical Dashboard

Static Next.js site using the dashboard template directly in `app/page.jsx`.

## Run locally

```bash
npm install
npm run dev
```

## Build the static site

```bash
npm run build
```

The exported site is written to `dashboard/out/` and can be served by any
static web host.
